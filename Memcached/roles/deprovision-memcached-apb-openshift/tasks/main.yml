#############################################################################
# Deprovision Memcahed cluster
#############################################################################

- name: Remove memcached cluster
  cloudformation:
    aws_access_key: "{{ aws_access_key }}"
    aws_secret_key: "{{ aws_secret_key }}"
    stack_name: "apb-sns-{{ stack_identifier }}"
    state: "absent"
    region: "{{ region }}"
